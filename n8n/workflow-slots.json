{
  "name": "Retell AI → Available Slots (Google Calendar)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "available-slots",
        "responseMode": "responseNode",
        "options": {
          "rawBody": false
        }
      },
      "id": "node-webhook",
      "name": "Webhook – Slot-Anfrage",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "available-slots"
    },
    {
      "parameters": {
        "method": "GET",
        "url": "https://www.googleapis.com/calendar/v3/calendars/primary/events",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "timeMin",
              "value": "={{ $now.toISO() }}"
            },
            {
              "name": "timeMax",
              "value": "={{ $now.plus({ days: 14 }).toISO() }}"
            },
            {
              "name": "singleEvents",
              "value": "true"
            },
            {
              "name": "orderBy",
              "value": "startTime"
            },
            {
              "name": "maxResults",
              "value": "250"
            }
          ]
        },
        "options": {}
      },
      "id": "node-gcal-fetch",
      "name": "HTTP – Google Calendar Events abrufen",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [480, 300],
      "credentials": {
        "oAuth2Api": {
          "id": "REPLACE_WITH_CREDENTIAL_ID",
          "name": "Google Calendar OAuth2"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const calendarResponse = $input.first().json;\nconst events = (calendarResponse.items || []).map(e => ({\n  start: new Date(e.start.dateTime || e.start.date),\n  end: new Date(e.end.dateTime || e.end.date)\n}));\n\n// Debug: log Google Calendar query range\nconst now = new Date();\nconsole.log('DEBUG timeMin:', now.toISOString());\nconsole.log('DEBUG timeMax:', new Date(now.getTime() + 14*24*60*60*1000).toISOString());\nconsole.log('DEBUG events fetched:', events.length);\n\n// Berliner Feiertage (feste + bewegliche via Gauss-Osterformel)\nfunction getBerlinHolidays(year) {\n  const holidays = [\n    `${year}-01-01`,`${year}-03-08`,`${year}-05-01`,\n    `${year}-10-03`,`${year}-12-25`,`${year}-12-26`,\n  ];\n  const a=year%19,b=Math.floor(year/100),c=year%100;\n  const d=Math.floor(b/4),e=b%4,f=Math.floor((b+8)/25);\n  const g=Math.floor((b-f+1)/3),h=(19*a+b-d-g+15)%30;\n  const i=Math.floor(c/4),k=c%4,l=(32+2*e+2*i-h-k)%7;\n  const m=Math.floor((a+11*h+22*l)/451);\n  const mo=Math.floor((h+l-7*m+114)/31);\n  const da=((h+l-7*m+114)%31)+1;\n  const easter=new Date(year,mo-1,da);\n  const add=(d,n)=>{const r=new Date(d);r.setDate(r.getDate()+n);return r;};\n  const fmt=(d)=>d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');\n  [-2,1,39,50].forEach(o=>holidays.push(fmt(add(easter,o))));\n  return new Set(holidays);\n}\n\nconst nowBerlin = new Date(now.toLocaleString('en-US',{timeZone:'Europe/Berlin'}));\nconst holidays = getBerlinHolidays(nowBerlin.getFullYear());\ngetBerlinHolidays(nowBerlin.getFullYear()+1).forEach(h=>holidays.add(h));\n\nconst allSlots = [];\nconst slotDuration = 20;\nconst maxSlots = 10;\nconst maxDays = 14;\nconst dayNames = ['Sonntag','Montag','Dienstag','Mittwoch','Donnerstag','Freitag','Samstag'];\nconst monthNames = ['Januar','Februar','März','April','Mai','Juni','Juli','August','September','Oktober','November','Dezember'];\n\nfor (let dayOffset = 0; dayOffset < maxDays; dayOffset++) {\n  const checkDate = new Date(nowBerlin);\n  checkDate.setDate(checkDate.getDate() + dayOffset);\n  checkDate.setHours(0,0,0,0);\n\n  const wd = checkDate.getDay();\n  if (wd === 0 || wd === 6) continue;\n\n  const dateKey = checkDate.getFullYear()+'-'+String(checkDate.getMonth()+1).padStart(2,'0')+'-'+String(checkDate.getDate()).padStart(2,'0');\n  if (holidays.has(dateKey)) continue;\n\n  const displayDate = dayNames[wd]+', '+checkDate.getDate()+'. '+monthNames[checkDate.getMonth()];\n  const morningSlots = [];\n  const afternoonSlots = [];\n\n  // Scan full day 09:00-18:00, collect ALL free slots into morning/afternoon buckets\n  for (let hour = 9; hour < 18; hour++) {\n    for (let minute = 0; minute < 60; minute += slotDuration) {\n      const slotBerlin = new Date(checkDate);\n      slotBerlin.setHours(hour, minute, 0, 0);\n      const slotEndBerlin = new Date(slotBerlin);\n      slotEndBerlin.setMinutes(slotEndBerlin.getMinutes() + slotDuration);\n\n      // Last slot must end by 18:00\n      const endLimit = new Date(checkDate);\n      endLimit.setHours(18, 0, 0, 0);\n      if (slotEndBerlin > endLimit) continue;\n      if (slotBerlin <= nowBerlin) continue;\n\n      const isoStart = dateKey+'T'+String(hour).padStart(2,'0')+':'+String(minute).padStart(2,'0')+':00+02:00';\n      const endH = slotEndBerlin.getHours();\n      const endM = slotEndBerlin.getMinutes();\n      const isoEnd = dateKey+'T'+String(endH).padStart(2,'0')+':'+String(endM).padStart(2,'0')+':00+02:00';\n      const startUTC = new Date(isoStart);\n      const endUTC = new Date(isoEnd);\n\n      const hasConflict = events.some(ev => startUTC < ev.end && endUTC > ev.start);\n      if (!hasConflict) {\n        const slot = { date: displayDate, time: String(hour).padStart(2,'0')+':'+String(minute).padStart(2,'0'), start: isoStart, end: isoEnd, slot_id: isoStart };\n        if (hour < 13) { morningSlots.push(slot); } else { afternoonSlots.push(slot); }\n      }\n    }\n  }\n\n  // Pick up to 2 morning + 2 afternoon per day (balanced mix)\n  if (morningSlots.length > 0) allSlots.push(morningSlots[0]);\n  if (afternoonSlots.length > 0) allSlots.push(afternoonSlots[0]);\n  if (morningSlots.length > 1) allSlots.push(morningSlots[Math.floor(morningSlots.length / 2)]);\n  if (afternoonSlots.length > 1) allSlots.push(afternoonSlots[Math.floor(afternoonSlots.length / 2)]);\n}\n\nconst result = allSlots.slice(0, maxSlots);\nconsole.log('DEBUG slots returned:', result.length, '| total collected:', allSlots.length);\nconsole.log('DEBUG sample slots:', JSON.stringify(result.slice(0, 4)));\nreturn [{json:{slots:result,count:result.length}}];"
      },
      "id": "node-find-slots",
      "name": "Code – Freie Slots berechnen",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [720, 300]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              }
            ]
          }
        }
      },
      "id": "node-response",
      "name": "Response – Verfügbare Slots",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [960, 300]
    }
  ],
  "connections": {
    "Webhook – Slot-Anfrage": {
      "main": [
        [
          {
            "node": "HTTP – Google Calendar Events abrufen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP – Google Calendar Events abrufen": {
      "main": [
        [
          {
            "node": "Code – Freie Slots berechnen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code – Freie Slots berechnen": {
      "main": [
        [
          {
            "node": "Response – Verfügbare Slots",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [
    { "name": "retell-ai", "id": "tag-retell" },
    { "name": "voice-agent", "id": "tag-voice" },
    { "name": "calendar", "id": "tag-calendar" }
  ]
}
